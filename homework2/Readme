在实现SQL类的作业未完成，
# update的思路：
> 先将文件都存入一个字典，
> 通过对update语句进行两次split（第一次用空格，第二次用=），读取到需要修改的字段和值
> 分析后面的where，这是一个难题，如何判断where后面有几个参数,每个参数是什么（如，where name=xxx and age = xxx and id = x ），并在文件中进行对比。
> 将字典如何转化成要求格式的字符串，并存入到文件中

# insert时遇到的问题
> 都知道insert有两种写法一种时key=value的写法，一种是只写值的写法，针对这两种写法都需要进行判断
> 实现insert只需要在文件的最后添加行，可以使用write方法

